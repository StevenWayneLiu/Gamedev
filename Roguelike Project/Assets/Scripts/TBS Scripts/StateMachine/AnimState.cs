/*
 * State used when playing any kind of animation where input is turned off
 */

using UnityEngine;
using System.Collections;

public class AnimState : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
